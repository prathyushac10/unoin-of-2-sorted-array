
class Solution:
    
    #Function to return a list containing the union of the two arrays.
    def findUnion(self,a,b):
        # code here
        s=a+b
        d=set(s)
        t=[]
        for i in d:
            t.append(i)
        t.sort()
        return t
